SQLAlchemy Repository Class
=====================

The repository class allows simplified CRUD operations against a SQLAlchemy model.

SQLAlchemy Declarative Models
=====================

todo:
`orm.Base`
`orm.AuditColumn`
`orm.dict_to_model`
`type_encoders`
