# Allowed Hosts Config

::: starlite.config.AllowedHostsConfig
    options:
        members:
            - allowed_hosts
            - exclude
            - exclude_opt_key
            - scopes
            - www_redirect
