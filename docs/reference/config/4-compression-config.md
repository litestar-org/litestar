# Compression Config

::: starlite.config.compression.CompressionConfig
    options:
        members:
            - backend
            - brotli_gzip_fallback
            - brotli_lgblock
            - brotli_lgwin
            - brotli_mode
            - brotli_quality
            - exclude
            - exclude_opt_key
            - gzip_compress_level
            - middleware_class
            - minimum_size
